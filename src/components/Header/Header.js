import React from 'react';
import { LoginContainer } from '../index';

const Header = () => {
  return (
    <header>
      <h1>Wall App</h1>
      <LoginContainer />
    </header>
  );
};

export default Header;
