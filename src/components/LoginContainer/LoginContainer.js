import React from 'react';
import { Button } from '../';

const LoginContainer = () => {
  return (
    <div>
      <input type="text" />
      <input type="text" />
      <Button text="Sing in" />
      <Button text="Sing up" />
    </div>
  );
};

export default LoginContainer;
