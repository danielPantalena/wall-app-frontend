import React from 'react';
import { Post, LoginContainer } from '../../components';

const Main = () => {
  return (
    <div>
      <LoginContainer />
      <Post />
    </div>
  );
};

export default Main;
